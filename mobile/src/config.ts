// mobile/src/config.ts
// TODO: Configure this properly with environment variables if using Expo build profiles or EAS Build.
// For now, using a placeholder or a simple default.
export const API_BASE_URL = 'http://localhost:3000/api'; // Default for local dev, adjust as needed

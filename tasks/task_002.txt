# Task ID: 2
# Title: Implement Shared Type Definitions
# Status: pending
# Dependencies: None
# Priority: high
# Description: Create a shared type system between backend and frontend to maintain type safety during the migration.
# Details:
1. Create or update the `@fitnessai/types` package with shared interfaces
2. Implement Zod schemas for request/response validation
3. Export TypeScript types derived from Zod schemas

Example implementation:
```typescript
// packages/types/src/training.ts
import { z } from 'zod';

export const TrainingPlanSchema = z.object({
  id: z.string().uuid(),
  userId: z.string().uuid(),
  name: z.string(),
  weeks: z.array(z.object({
    weekNumber: z.number(),
    sessions: z.array(z.object({
      day: z.number(),
      exercises: z.array(z.object({
        name: z.string(),
        sets: z.number(),
        reps: z.number(),
        weight: z.number().optional()
      }))
    }))
  })),
  createdAt: z.string().datetime(),
  updatedAt: z.string().datetime()
});

export type TrainingPlan = z.infer<typeof TrainingPlanSchema>;

// Similar schemas for feedback, suggestions, etc.
```

# Test Strategy:
1. Write unit tests for schema validation
2. Test edge cases for each schema
3. Verify type inference works correctly
4. Test schema composition for complex objects

# Subtasks:
## 1. Set up project structure for shared types [pending]
### Dependencies: None
### Description: Create a new directory for shared types and configure TypeScript
### Details:
Create a 'shared-types' directory, initialize npm, install TypeScript and Zod, and set up tsconfig.json

## 2. Define Zod schemas for core data structures [pending]
### Dependencies: 2.1
### Description: Create Zod schemas for main entities and data types used in the application
### Details:
Identify core data structures, create Zod schemas for each, ensuring proper validation rules

## 3. Generate TypeScript types from Zod schemas [pending]
### Dependencies: 2.2
### Description: Use Zod's inference to create TypeScript types from the defined schemas
### Details:
Create a types.ts file, import Zod schemas, and use z.infer<typeof Schema> to generate TypeScript types

## 4. Document shared types and usage guidelines [pending]
### Dependencies: 2.3
### Description: Create documentation for the shared types and how to use them in different parts of the application
### Details:
Write README.md with explanations of each type, usage examples, and best practices for type safety

## 5. Integrate shared types into backend and frontend [pending]
### Dependencies: 2.3, 2.4
### Description: Import and use the shared types in both backend and frontend code
### Details:
Update import statements in relevant files, refactor existing type annotations, and ensure type consistency across the application


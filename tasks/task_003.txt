# Task ID: 3
# Title: Implement Authentication REST Endpoints
# Status: done
# Dependencies: 1, 2
# Priority: high
# Description: Create REST endpoints for authentication that mirror the existing gRPC authentication service.
# Details:
1. Create authentication routes in `/api/auth`
2. Implement login, registration, and token refresh endpoints
3. Ensure JWT token generation matches existing gRPC implementation
4. Implement proper HTTP status codes for auth failures

Example implementation:
```typescript
// src/api/rest/routes/auth.ts
import { FastifyInstance } from 'fastify';
import { AuthService } from '../../../services/auth';
import { LoginSchema, RegisterSchema } from '@fitnessai/types';

export async function authRoutes(fastify: FastifyInstance) {
  const authService = new AuthService();
  
  fastify.post('/login', {
    schema: {
      body: LoginSchema
    },
    handler: async (request, reply) => {
      try {
        const { email, password } = request.body;
        const result = await authService.login(email, password);
        return reply.status(200).send(result);
      } catch (error) {
        if (error.message === 'Invalid credentials') {
          return reply.status(401).send({
            error: 'AuthenticationError',
            message: 'Invalid email or password'
          });
        }
        throw error;
      }
    }
  });
  
  // Similar endpoints for register, refresh-token, etc.
}
```

# Test Strategy:
1. Unit test each authentication endpoint
2. Test successful login/registration flows
3. Test error cases (invalid credentials, already registered)
4. Verify JWT token generation and validation
5. Test token refresh functionality

# Subtasks:
## 1. Create authentication route structure [done]
### Dependencies: None
### Description: Set up the basic structure for authentication routes in `/api/auth`
### Details:
Create a new file `auth.ts` in the `/api/rest/routes` directory. Define and export an async function `authRoutes` that takes a FastifyInstance parameter. Import necessary dependencies like FastifyInstance and AuthService.
<info added on 2025-05-25T11:06:09.039Z>
File server/src/api/rest/routes/auth.ts has been created with placeholder routes. The AuthService import and instantiation are currently commented out due to the missing service file. This will need to be addressed when implementing the specific endpoints. Remember to uncomment and properly import the AuthService once it's available.
</info added on 2025-05-25T11:06:09.039Z>

## 2. Implement login endpoint [done]
### Dependencies: 3.1
### Description: Create a POST endpoint for user login that mirrors the gRPC implementation
### Details:
Inside the `authRoutes` function, implement a POST route for '/login'. Use the LoginSchema for request body validation. Call the AuthService's login method with email and password. Return a JWT token on successful login. Implement proper error handling for invalid credentials.
<info added on 2025-05-25T11:10:30.568Z>
Placeholder login endpoint implemented in `server/src/api/rest/routes/auth.ts`. The `AuthService` import is currently commented out due to the file not being found. The `LoginRequestSchema` and `LoginRequest` are imported from 'shared-types'. If linter errors persist for this import, review and update the server project's `package.json` and `tsconfig.json` to correctly resolve the local `shared-types` package. This may involve adding it as a local file dependency and configuring `baseUrl` or `paths`. Next steps: Implement the `AuthService`, uncomment its import, and complete the login endpoint functionality.
</info added on 2025-05-25T11:10:30.568Z>
<info added on 2025-05-25T11:14:00.548Z>
The login endpoint in `server/src/api/rest/routes/auth.ts` has been updated with the following changes:
1. The `AuthService` is now imported from `server/src/services/auth.ts` and utilized in the endpoint.
2. The `authService.login()` method is called with proper success and error handling implemented.
3. Shared types are now imported from '@shared-types/user'.
4. If linter errors persist for '@shared-types/user', it's recommended to review and update the server project's `package.json` and `tsconfig.json` to correctly resolve this local package. This may involve configuring `baseUrl` or `paths`.

Next steps: Ensure the `AuthService` implementation in `server/src/services/auth.ts` is complete and functional. Verify that the login endpoint works as expected, returning a JWT token on successful login and handling errors appropriately.
</info added on 2025-05-25T11:14:00.548Z>

## 3. Implement registration endpoint [done]
### Dependencies: 3.1
### Description: Create a POST endpoint for user registration that mirrors the gRPC implementation
### Details:
Inside the `authRoutes` function, implement a POST route for '/register'. Use the RegisterSchema for request body validation. Call the AuthService's register method with user details. Return a success message or user object on successful registration. Implement proper error handling for existing users or invalid data.
<info added on 2025-05-25T11:15:01.779Z>
The registration endpoint has been implemented in `server/src/api/rest/routes/auth.ts`. It uses `RegisterRequestSchema` for request body validation and calls the placeholder `authService.register()` method. On successful registration, it returns a 201 status. Basic error handling is in place, with 409 status for conflicts (e.g., existing users) and 500 for server errors. The shared types are imported from '@shared-types/user'. Note: If linter errors persist for this import, the project configuration for resolving this local package should be reviewed.
</info added on 2025-05-25T11:15:01.779Z>

## 4. Implement token refresh endpoint [done]
### Dependencies: 3.1
### Description: Create a POST endpoint for refreshing JWT tokens
### Details:
Inside the `authRoutes` function, implement a POST route for '/refresh-token'. Validate the incoming refresh token. Call the AuthService's refreshToken method. Return a new access token on successful refresh. Implement proper error handling for invalid or expired refresh tokens.
<info added on 2025-05-25T11:16:03.122Z>
The token refresh endpoint has been implemented in `server/src/api/rest/routes/auth.ts`. It uses an inline Zod schema (`RefreshTokenRequestSchema`) for request body validation and calls `authService.refreshToken()`. The implementation includes success (200) and error handling (401 for invalid token, 500 for server error). The Zod import (`import { z } from 'zod';`) was added to the file. If linter errors for 'zod' module not found persist, ensure Zod is installed and resolvable in the server project environment.
</info added on 2025-05-25T11:16:03.122Z>

## 5. Implement authentication middleware [done]
### Dependencies: 3.2, 3.3, 3.4
### Description: Create middleware for verifying JWT tokens on protected routes
### Details:
Create a new file `authMiddleware.ts` in the `/api/rest/middleware` directory. Implement a function that verifies the JWT token from the Authorization header. Use the same token verification logic as in the gRPC implementation. Return 401 Unauthorized for invalid tokens. Attach the decoded user information to the request object for use in protected routes.
<info added on 2025-05-25T11:18:15.116Z>
The `authMiddleware.ts` file has been created in `server/src/api/rest/middleware/`. The middleware implements JWT token verification using `request.server.jwt.extractToken` and `request.server.jwt.verify` from the existing `jwt.ts` plugin. On successful verification, it attaches an `AuthenticatedUser` object (derived from `JwtPayload`) to `request.user`. For failed authentication, it sends a 401 error. The next step is to register this middleware in the Fastify application for protected routes.
</info added on 2025-05-25T11:18:15.116Z>


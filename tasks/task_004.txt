# Task ID: 4
# Title: Implement User Profile REST Endpoints
# Status: pending
# Dependencies: 1, 2, 3
# Priority: medium
# Description: Create REST endpoints for user profile operations that mirror the existing gRPC user service.
# Details:
1. Create user profile routes in `/api/user`
2. Implement GET and PUT endpoints for profile management
3. Ensure proper authentication middleware is applied

Example implementation:
```typescript
// src/api/rest/routes/user.ts
import { FastifyInstance } from 'fastify';
import { UserService } from '../../../services/user';
import { UpdateProfileSchema } from '@fitnessai/types';
import { authenticate } from '../middleware/auth';

export async function userRoutes(fastify: FastifyInstance) {
  const userService = new UserService();
  
  fastify.get('/profile', {
    preHandler: [authenticate],
    handler: async (request, reply) => {
      const userId = request.user.id;
      const profile = await userService.getUserProfile(userId);
      if (!profile) {
        return reply.status(404).send({
          error: 'NotFound',
          message: 'User profile not found'
        });
      }
      return reply.send(profile);
    }
  });
  
  fastify.put('/profile', {
    schema: {
      body: UpdateProfileSchema
    },
    preHandler: [authenticate],
    handler: async (request, reply) => {
      const userId = request.user.id;
      const updatedProfile = await userService.updateProfile(userId, request.body);
      return reply.send(updatedProfile);
    }
  });
}
```

# Test Strategy:
1. Unit test each user profile endpoint
2. Test authentication middleware integration
3. Test successful profile retrieval and updates
4. Test error cases (user not found, validation errors)
5. Verify database interactions

# Subtasks:
## 1. Set up route file for /api/user [pending]
### Dependencies: None
### Description: Create a new file for user profile routes and set up the basic structure
### Details:
Create a new file 'src/api/rest/routes/user.ts'. Import necessary modules (FastifyInstance, UserService). Define and export an async function 'userRoutes' that takes a FastifyInstance parameter.

## 2. Create placeholder UserService [pending]
### Dependencies: None
### Description: Implement a basic UserService class with placeholder methods for profile operations
### Details:
Create 'src/services/user.ts'. Define UserService class with methods: getUserProfile(userId: string) and updateProfile(userId: string, data: any). Implement these methods with placeholder functionality (e.g., return mock data).

## 3. Implement GET /profile endpoint [pending]
### Dependencies: 4.1, 4.2
### Description: Create a GET endpoint for retrieving user profiles
### Details:
In userRoutes function, add fastify.get('/profile', ...) route. Use authenticate middleware. Implement handler to call userService.getUserProfile(). Return 404 if profile not found, otherwise return profile data.

## 4. Implement PUT /profile endpoint [pending]
### Dependencies: 4.1, 4.2
### Description: Create a PUT endpoint for updating user profiles
### Details:
In userRoutes function, add fastify.put('/profile', ...) route. Use authenticate middleware and UpdateProfileSchema for request body validation. Implement handler to call userService.updateProfile(). Return updated profile data.

## 5. Apply authentication middleware [pending]
### Dependencies: 4.3, 4.4
### Description: Ensure authentication middleware is correctly applied to both endpoints
### Details:
Review both GET and PUT /profile routes. Confirm 'authenticate' middleware is included in the 'preHandler' array for each route. Test that unauthenticated requests are properly rejected.

